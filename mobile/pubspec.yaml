name: mobile
description: "A new Flutter project."
publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: ^3.10.1

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  http: ^1.6.0
  go_router: ^17.0.1
  url_launcher: ^6.3.2
  image_picker: ^1.2.1
  flutter_map: ^8.2.2
  latlong2: ^0.9.1
  geolocator: ^14.0.2
  shared_preferences: ^2.5.4
  google_mobile_ads: ^7.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  # --- OUTILS POUR LE DESIGN ---
  flutter_launcher_icons: ^0.13.1
  flutter_native_splash: ^2.3.1

flutter:
  uses-material-design: true

  # LISTE DES IMAGES
  assets:
    - assets/car.jpg
    - assets/house.jpg
    - assets/hotel.jpg
    - assets/logo.png  # <--- J'AI AJOUTÉ VOTRE LOGO ICI

# ==========================================
# CONFIGURATION DE L'ICÔNE DE L'APP 📱
# ==========================================
flutter_launcher_icons:
  android: "launcher_icon"
  ios: true
  image_path: "assets/logo.png"
  min_sdk_android: 21

# ==========================================
# CONFIGURATION DE L'ÉCRAN DE DÉMARRAGE 🚀
# ==========================================
flutter_native_splash:
  color: "#003580" # La couleur bleue de fond
  image: "assets/logo.png" # Le logo au centre
  android_12:
    image: "assets/logo.png"
    color: "#003580"